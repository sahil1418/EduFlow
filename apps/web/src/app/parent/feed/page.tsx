// Parent feed — backend already returns only the linked child's section's
// posts (+ school-wide) for PARENT role. Same read-only UI as the student
// portal — re-export.
export { default } from '@/app/student/feed/page';
