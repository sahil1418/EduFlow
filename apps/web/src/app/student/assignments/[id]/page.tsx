// Student assignment detail — the existing rich page already gates by role
// (STUDENT sees submit form; teachers see grading). Re-export.
export { default } from '@/app/(app)/assignments/[id]/page';
