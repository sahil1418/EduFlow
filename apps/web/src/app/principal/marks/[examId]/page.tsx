// Marks-entry detail is identical for Principal vs Teacher — re-export the
// existing rich page from the legacy (app) tree. When the legacy tree is
// retired, the implementation moves here.
export { default } from '@/app/(app)/marks/[examId]/page';
